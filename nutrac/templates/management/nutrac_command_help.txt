nutrac <sub-command>

Sub-commands are:

      install        Install the application (i.e. database, paths, configurations, etc.)
      list           List nutrac or trac objects
      create         Crate nutrac or trac objects